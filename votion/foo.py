"""Foo module."""


def plus(one: int, two: int) -> int:
    """Calculate sum of arguments.

    Args:
        one: First num to add
        two: Second num to add

    Returns:
        Sum of arguments
    """
    return one + two
